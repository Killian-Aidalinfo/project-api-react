{
  "name": "project-api-react",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts",
    "test": "bun test",
    "build": "bun build index.ts --target bun --outdir dist"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
